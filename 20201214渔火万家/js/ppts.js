$(document).ready(function () {
  $('#fullpage').fullpage({
    // navigation: true,
    // scrollBar: true,
    navigationPosition: screenLeft,
    anchors: ['page0', 'page1', 'page2', 'page3'],
  });
}); 